/*
Debemos lograr tomar un nombre con 'prompt' 
y luego mostrarlo por 'alert' al presionar el botón  'mostrar'*/
function mostrar()
{
	//declaracion de variable, la variable solo empieza por letras o signos, NO con numeros
	//var nombre;//camel case


	//var nombreCompleto;

	//las variables tienen que ser representativa

	//como guardamos los datos en la variable, tiene que estar primero declarada, operador de asignacion para guardar un valor a la variable es el signo "=", este operador le va a indicar que lo que esta a la derecha (siempre entre comillas), o sea un valor, lo guarde a la izquierda.

	//nombre = "mario;"//string

	//nombre = 1; //number

	//nombre = true; //boolean

	//nombre = null; //null, tambien dice que no tiene valor la variable, se lo decimos de manera explicita

	// typeof nos dice de que tipo es una variable

	//las variables tienen un valor y un tipo

	var dato;

	dato = prompt("ingrese un dato");

	alert(dato);

	
}

